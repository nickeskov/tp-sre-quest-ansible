server {
    listen           80 default_server;
    access_log        off;
    log_not_found     off;
    server_name       _;

    return 444;
}

server {
    listen            80;
    server_name       "";
    access_log        off;
    log_not_found     off;

    return 444;
}
